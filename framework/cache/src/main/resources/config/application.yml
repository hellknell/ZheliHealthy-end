spring:
  cache:
    type: redis
    redis:
#    缓存时间
      time-to-live: 2592000000
  redis:
    database: 0
    host: localhost
    port: 6379
    password: abc123456
    jedis:
      pool:
        max-active: 1000
        max-wait: -1ms
        max-idle: 16
        min-idle: 8